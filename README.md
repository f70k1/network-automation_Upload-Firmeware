# vnetwork-automation_Upload-Firmeware
Based on the version of the network component, the corresponding firmware (Firmware.csv) is transferred via scp.
The IP address is processed sequentially from an arbitrary list (ip-device.csv).
